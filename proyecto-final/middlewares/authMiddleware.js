//* Entiendo que lo que se tiene que hacer para ver si está autenticado va a ser un token JWT
//* por eso lo separo a este archivo como si fuese un middleware

const isAuthenticated = false


export default 
export default isAuthenticated